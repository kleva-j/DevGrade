# PostgreSQL connection string (used in every environment — dev, CI, prod).
# Local example (matches apps/web/docker-compose.yml credentials):
DATABASE_URL="postgres://devgrade:devgrade@localhost:5432/devgrade"
